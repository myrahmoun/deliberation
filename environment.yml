name: deliberation
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip
  - pytorch>=2.0
  - pytorch-cuda=12.1
  - numpy>=1.24
  - pandas>=2.0
  - matplotlib>=3.7
  - pip:
      - sentence-transformers>=3.0
      - peft>=0.10
      - safetensors>=0.4
